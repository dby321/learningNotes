\documentclass[UTF8,12pt,letterpaper]{ctexart}\title{My First Document}
\author{千里水草}
\date{\today}
\begin{document}
\maketitle
你好，世界！你好
nihao
% 这是一行注释
% 类似 HTML 中的 <b> 标签 —— 加粗文本
\textbf{加粗文本}

%类似 HTML 中的 <i> 标签 —— 斜体文本
\textit{斜体文本}

% 类似 HTML 中的 <tt> 或 <code> 标签 —— 等宽字体
\texttt{等宽字体文本}

% 类似 HTML 中的 <u> 标签 —— 下划线文本 
\underline{下划线文本}
% 类似 HTML 中的 <em> 标签 —— 强调文本
Some of the greatest \emph{discoveries} in science 
were made by accident.

\textit{Some of the greatest \emph{discoveries} 
in science were made by accident.}

\textbf{Some of the greatest \emph{discoveries} 
in science were made by accident.}

% 类似 HTML 中的 <sub> 标签 —— 下标文本
This is an example of \textsubscript{a subscript}.

% 类似 HTML 中的 <sup> 标签 —— 上标文本
This is an example of \textsuperscript{a superscript}

% 类似 HTML 中的 <small> 标签 —— 小号文本
\small{This is a small text.}

{\large This is a large text.}

{\Large This is a larger text.}

{\huge This is a huge text.}



\end{document}