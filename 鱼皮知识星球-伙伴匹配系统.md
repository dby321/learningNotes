# 鱼皮知识星球-伙伴匹配系统

猫十二学习笔记 http://yuque.com/shierkcs/catstudy/tswr4kn0r9mo8glt

沙鱼学习笔记 https://www.yuque.com/shayushitiande/cfl9ih/qrlktb

## 技术选型

**前端**

- Vue3
- Vant UI组件库
- Vite脚手架
- Axios请求库

**后端**

- Java SpringBoot
- MySQL
- MyBatis-Plus
- MyBatisX自动生成代码
- Redis缓存
- Redisson分布式锁
- EasyExcel数据导入
- SpringScheduler定时任务
- Gson
- 相似度匹配算法

**部署**

- serverless服务
- 云原生容器平台

## 前端项目初识化

- 使用脚手架初始化项目

  - VueCli 

  - Vite脚手架（可以初始化不同框架的前端项目,本次使用这个）

- `yarn add Vant4`

## axios处理

```js
import axios from "axios";
import qs from 'qs';
export const request = axios.create({
    baseURL: 'http://localhost:8080/api/',
    timeout: 10000,
    paramsSerializer: {
        serialize: (params) =>
            qs.stringify(params, { arrayFormat: 'repeat' }) // 使用重复键名格式
    }
});
```

## redis配置

https://juejin.cn/post/7527275686416941108

https://blog.csdn.net/xujunfei520/article/details/106567366

```java
@Configuration
public class RedisConfig {

    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(factory);

        // 使用Jackson2JsonRedisSerializer来序列化和反序列化redis的value值
        Jackson2JsonRedisSerializer<Object> serializer = new Jackson2JsonRedisSerializer<>(Object.class);
        ObjectMapper mapper = new ObjectMapper();
        mapper.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);
        mapper.activateDefaultTyping(mapper.getPolymorphicTypeValidator(), ObjectMapper.DefaultTyping.NON_FINAL);
        serializer.setObjectMapper(mapper);

        // 使用StringRedisSerializer来序列化和反序列化redis的key值
        template.setKeySerializer(new StringRedisSerializer());
        template.setValueSerializer(serializer);

        // Hash key/value序列化方式
        template.setHashKeySerializer(new StringRedisSerializer());
        template.setHashValueSerializer(serializer);

        template.afterPropertiesSet();
        return template;
    }
}
```

## 业务实现

### 关注功能







